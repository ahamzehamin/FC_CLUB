# فیتمس - پروژه آموزشی کامل MERN Stack 🚀

## 🌟 **پروژه آموزشی جامع MERN Stack برای توسعه دهندگان**

این پروژه یک راهنمای کامل یادگیری برای توسعه وب مدرن است. پروژه فیتمس نمونه کاملی از تبدیل یک قالب HTML استاتیک به یک برنامه دینامیک و کامل MERN Stack با معماری سازمانی است.

![MERN Stack](https://img.shields.io/badge/MERN-Stack-green) ![React](https://img.shields.io/badge/React-18.2.0-blue) ![Node.js](https://img.shields.io/badge/Node.js-18+-green) ![MongoDB](https://img.shields.io/badge/MongoDB-7.0-green) ![Express](https://img.shields.io/badge/Express-4.18.2-black)

---

## 📚 **چه چیزی از این پروژه یاد خواهید گرفت؟**

### 🎯 **مفاهیم پایه MERN Stack**
- **ادغام MongoDB**: طراحی اسکیما، روابط داده‌ها، اعتبار سنجی، ایندکس‌گذاری برای عملکرد بهتر
- **معماری Express.js**: طراحی RESTful API، الگوهای middleware، مدیریت خطا، سازماندهی مسیرها
- **طراحی کامپوننت React**: مدیریت state با hooks، چرخه حیات، ترکیب کامپوننت‌ها، انتخاب بین prop drilling و context
- **توسعه Node.js**: راه‌اندازی سرور، تنظیمات محیطی، مدیریت پکیج، الگوهای برنامه‌نویسی asynchronous

### 🏗️ **مهارت‌های توسعه Full-Stack**
- **طراحی پایگاه داده**: ایجاد مدل‌های داده قابل مقیاس، درک روابط document، پیاده‌سازی اعتبار سنجی در سطح اسکیما
- **توسعه API**: ساخت APIهای RESTful کامل با متدهای HTTP مناسب، کدهای وضعیت، صفحه‌بندی، فیلترینگ و مرتب‌سازی
- **ادغام Frontend-Backend**: اتصال کامپوننت‌های React به endpointهای Express با استفاده از Axios، مدیریت loading states، boundaryهای خطا
- **استراتژی Deployment**: معماری برنامه آماده تولید، مدیریت محیط، ملاحظات امنیتی

### 📈 **پрактиک‌های حرفه‌ای توسعه**
- **ساختار پروژه**: سازماندهی فولدر استاندارد صنعت، جداسازی مسئولیت‌ها، معماری مدولار
- **کیفیت کد**: کد تمیز، قابل نگهداری، مستندسازی شده، قراردادهای نام‌گذاری ثابت، فرمت‌بندی کد
- **مدیریت خطا**: مدیریت جامع خطا و logging، graceful degradation، پیام‌های خطای کاربرپسند
- **امنیت**: اعتبار سنجی ورودی، متغیرهای محیطی، تنظیمات CORS، محدودسازی نرخ، authentication و authorization
- **کارایی**: ایندکس‌گذاری پایگاه داده، بهینه‌سازی پاسخ‌های API، الگوهای کارایی React، lazy loading
- **تست**: تست واحد، تست integration، استراتژی‌های تست end-to-end
- **کنترل نسخه**: workflow گیت، استراتژی‌های branching، قراردادهای commit
- **مستندسازی**: مستندسازی API، کامنت‌های کد، نگهداری README

### 🎓 **مفاهیم پیشرفته یادگیری**

#### **Authentication و Authorization**
- **پیاده‌سازی JWT**: authentication مبتنی بر توکن، refresh tokens، مدیریت امن cookie
- **Role-Based Access Control**: مجوزهای admin، editor، viewer با کنترل granular
- **امنیت رمز عبور**: hashing با bcrypt، اعتبار سنجی قدرت رمز عبور، جریان‌های secure password reset
- **مدیریت Session**: جریان‌های login/logout، persistence session، refresh خودکار توکن

#### **آپلود فایل و مدیریت رسانه**
- **ادغام Multer**: مدیریت آپلود فایل، تنظیمات storage، اعتبار سنجی نوع فایل
- **بهینه‌سازی تصویر**: فشرده‌سازی فایل، تبدیل فرمت، سرویس‌دهی responsive image
- **Cloud Storage**: الگوهای ادغام AWS S3، Cloudinary، یا filesystem محلی

#### **سیستم مدیریت محتوا (CMS)**
- **محتوای دینامیک**: مدیریت پست‌های بلاگ، اعضای تیم، پلن‌های قیمت‌گذاری از طریق پنل admin
- **ویرایش Rich Text**: ادغام CKEditor برای ایجاد محتوای بلاگ
- **بهینه‌سازی SEO**: meta tags، structured data، URL slugs، تولید sitemap
- **Workflow محتوا**: وضعیت‌های draft/published، زمان‌بندی محتوا، تاریخچه revision

#### **الگوهای معماری React**
- **Custom Hooks**: کپسوله کردن logic قابل استفاده مجدد، تبدیل رفتارهای jQuery به الگوهای React
- **Context API**: مدیریت global state برای authentication و تنظیمات app-wide
- **ترکیب کامپوننت**: ساخت UIهای پیچیده از کامپوننت‌های قابل استفاده مجدد
- **بهینه‌سازی کارایی**: React.memo، useMemo، useCallback، lazy loading، code splitting

#### **طراحی و معماری API**
- **RESTful Conventions**: URLهای مبتنی بر resource، متدهای HTTP، کدهای وضعیت، content negotiation
- **الگوهای Middleware**: middleware authentication، validation middleware، middleware مدیریت خطا
- **serialization داده**: فرمت‌های ثابت پاسخ API، metadata صفحه‌بندی، ساختار پاسخ‌های خطا
- **محدودسازی نرخ**: محافظت در برابر سوء استفاده، پیاده‌سازی محدودیت‌های مختلف برای endpointهای مختلف

#### **طراحی و بهینه‌سازی پایگاه داده**
- **طراحی Schema**: normalization در مقابل denormalization، embedding در مقابل referencing، استراتژی‌های ایندکس‌گذاری
- **روابط داده**: روابط one-to-one، one-to-many، many-to-many در MongoDB
- **بهینه‌سازی Query**: queryهای کارآمد، aggregation pipelines، مانیتورینگ کارایی پایگاه داده
- **Migration داده**: versioning schema، scriptهای migration، سازگاری backward

---

## 🏆 **دستاوردهای کلیدی پروژه**

### ✅ **تبدیل کامل وبسایت**
- **23 صفحه HTML** → **23 کامپوننت React**
- **قالب‌های Static** → **محتوای دینامیک Database-Driven**
- **Client-Side Routing** → **ادغام API Server-Side**
- **Deployment دستی** → **معماری آماده Production**

### ✅ **ویژگی‌های سطح سازمانی**
- **عملیات CRUD کامل** برای همه انواع محتوای اصلی
- **الگوهای پیشرفته React** (Hooks، Context، Higher-Order Components)
- **طراحی RESTful API** با کدهای وضعیت HTTP مناسب
- **روابط MongoDB** و اعتبار سنجی داده
- **طراحی Responsive** با حفظ قالب اصلی

---

## 💡 **چرا این پروژه مهم است؟**

### 🔧 **برنامه کاربردی واقعی**
این پروژه فقط یک آموزش نیست—یک **برنامه کامل و آماده production** است که نحوه تبدیل وبسایت‌های legacy static به برنامه‌های دینامیک مدرن را نشان می‌دهد. یاد خواهید گرفت چگونه:
- وبسایت‌های قدیمی را به برنامه‌های مدرن تبدیل کنید
- معماری full-stack حرفه‌ای پیاده‌سازی کنید
- برنامه‌های قابل مقیاس با جداسازی مناسب مسئولیت‌ها بسازید
- برنامه‌ها را برای محیط‌های production deploy کنید

### 🎓 **ارزش آموزشی**
- **مسیر یادگیری کامل**: از HTML static به full MERN stack
- **بهترین تمرین‌های صنعت**: سازماندهی کد حرفه‌ای و الگوها
- **JavaScript مدرن**: ES6+، async/await، destructuring، modules
- **معماری قابل مقیاس**: طراحی شده برای رشد و نگهداری

### 🚀 **توسعه شغلی**
- **پروژه Portfolio**: قابلیت‌های full-stack را به نمایش می‌گذارد
- **آماده‌سازی مصاحبه**: تخصص MERN stack را اثبات می‌کند
- **مهارت‌های آماده شغلی**: الگوهای سطح production را پیاده‌سازی می‌کند
- **استانداردهای حرفه‌ای**: کد تمیز، مستندسازی شده، قابل نگهداری

---

## 📋 **فهرست مطالب جامع**

- [📖 نمای کلی پروژه](#-نمای-کلی-پروژه)
- [🎯 آنچه یاد خواهید گرفت](#-آنچه-یاد-خواهید-گرفت-از-این-پروژه)
- [🛠 جزئیات عمیق Technology Stack](#-جزئیات-عمیق-technology-stack)
- [✨ ویژگی‌ها و قابلیت‌ها](#-ویژگیها-و-قابلیتها)
- [📁 معماری کامل پروژه](#-معماری-کامل-پروژه)
- [🚀 راه‌اندازی محیط توسعه](#-راهاندازی-محیط-توسعه)
- [🌐 راهنمای معماری Backend](#-راهنمای-معماری-backend)
- [⚛️ جزئیات عمیق React Frontend](#️-جزئیات-عمیق-react-frontend)
- [🔧 Custom React Hooks و تبدیل‌های jQuery](#-custom-react-hooks-و-تبدیلهای-jquery)
- [📡 مستندسازی REST API](#-مستندسازی-rest-api)
- [🗄️ طراحی پایگاه داده و Models](#️-طراحی-پایگاه-داده-و-models)
- [🔐 متغیرهای محیطی و امنیت](#-متغیرهای-محیطی-و-امنیت)
- [🧪 تست و کیفیت تضمین](#-تست-و-کیفیت-تضمین)
- [🚀 Deployment تولید](#-deployment-تولید)
- [🤝 مشارکت و توسعه](#-مشارکت-و-توسعه)
- [📄 لایسنس و Attribution](#-لایسنس-و-attribution)

---

## 📖 **نمای کلی پروژه**

### 🎯 **چالش**
یک قالب وبسایت تناسب اندام HTML زیبا را بگیرید و آن را به یک برنامه کاربردی کامل database-driven MERN stack تبدیل کنید و طراحی و تجربه کاربری اصلی را حفظ کنید.

### 🎯 **راه حل**
- **طراحی مجدد معماری کامل**: تبدیل فایل‌های static به سیستم‌های دینامیک
- **ادغام پایگاه داده**: جایگزینی محتوای hardcoded با collections MongoDB
- **توسعه API**: ساخت endpointهای RESTful برای مدیریت همه محتوا
- **Migration React**: تبدیل قالب‌های HTML به کامپوننت‌های performant React
- **آماده Production**: پیاده‌سازی مدیریت خطا، امنیت و تنظیمات deployment

### 🎯 **سفر یادگیری**
1. **Static به Dynamic**: درک تغییر از rendering سمت client به سمت server
2. **طراحی پایگاه داده**: ایجاد مدل‌های داده قابل مقیاس برای وب اپلیکیشن‌ها
3. **معماری API**: ساخت سرویس‌های RESTful با semantics HTTP مناسب
4. **معماری کامپوننت**: طراحی کامپوننت‌های React قابل استفاده مجدد
5. **مدیریت State**: مدیریت جریان داده بین frontend و backend
6. **استراتژی Deployment**: انتقال از توسعه به محیط‌های production

---

## 🎯 **آنچه یاد خواهید گرفت از این پروژه**

### 🔧 **مهارت‌های فنی توسعه**
- **Mastery JavaScript Full-Stack**: workflow کامل توسعه
- **الگوهای طراحی پایگاه داده**: ایجاد schemaهای robust و روابط
- **اصول طراحی API**: معماری RESTful و بهترین تمرین‌ها
- **اکوسیستم React**: Hooks، routing، مدیریت state، ترکیب کامپوننت
- **الگوهای Express.js**: Middleware، routing، مدیریت خطا، امنیت

### 🏗️ **معماری و الگوهای طراحی**
- **معماری MVC**: پیاده‌سازی الگوی Model-View-Controller
- **طراحی Component-Based**: کامپوننت‌های UI قابل استفاده مجدد و قابل نگهداری
- **جداسازی مسئولیت‌ها**: مرزهای واضح بین frontend و backend
- **ساختار پروژه قابل مقیاس**: سازماندهی فولدر استاندارد صنعت
- **استراتژی‌های مدیریت خطا**: مدیریت جامع خطا

### 🚀 **مهارت‌های توسعه Production**
- **مدیریت محیط**: تنظیمات development، staging، production
- **پیاده‌سازی امنیت**: اعتبار سنجی ورودی، CORS، جریان‌های authentication
- **بهینه‌سازی کارایی**: code splitting، lazy loading، استراتژی‌های caching
- **اتوماسیون Deployment**: pipelineهای CI/CD و deployment ابری
- **مانیتورینگ و Logging**: observability اپلیکیشن و debugging

---

## 🛠 **جزئیات عمیق Technology Stack**

### **معماری Backend Stack - Robust Server Architecture**

#### **Node.js Runtime Environment**
```javascript
// محیط runtime JavaScript مدرن برای توسعه server-side
const express = require('express');
const mongoose = require('mongoose');
const cors = require('cors');

// استفاده از ویژگی‌های ES6+، الگوهای async/await
const app = express();
app.use(cors());
app.use(express.json());
```

#### **Express.js Web Framework**
```javascript
// فریمورک وب سبک، انعطاف‌پذیر
app.get('/api/services', serviceController.getAllServices);
app.post('/api/services', serviceController.createService);
app.put('/api/services/:id', serviceController.updateService);
app.delete('/api/services/:id', serviceController.deleteService);
```

#### **MongoDB Document Database**
```javascript
// پایگاه داده NoSQL انعطاف‌پذیر برای documents شبه JSON
const serviceSchema = new mongoose.Schema({
  title: { type: String, required: true },
  description: String,
  features: [String],
  createdAt: { type: Date, default: Date.now }
});

const Service = mongoose.model('Service', serviceSchema);
```

### **معماری Frontend Stack - Modern React Architecture**

#### **React 18 با Hooks**
```javascript
// کامپوننت‌های functional مدرن با hooks
import React, { useState, useEffect } from 'react';

function Services() {
  const [services, setServices] = useState([]);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    fetchServices();
  }, []);

  const fetchServices = async () => {
    try {
      const response = await axios.get('/api/services');
      setServices(response.data.data);
    } catch (error) {
      console.error('Error fetching services:', error);
    } finally {
      setLoading(false);
    }
  };

  return (
    <div className="services-grid">
      {services.map(service => (
        <ServiceCard key={service._id} service={service} />
      ))}
    </div>
  );
}
```

#### **React Router برای ناوبری Client-Side**
```javascript
// routing declarative برای single-page applications
import { BrowserRouter as Router, Routes, Route } from 'react-router-dom';

function App() {
  return (
    <Router>
      <Routes>
        <Route path="/" element={<Home />} />
        <Route path="/services" element={<Services />} />
        <Route path="/contact" element={<Contact />} />
        {/* مسیرهای دینامیک تولید شده */}
      </Routes>
    </Router>
  );
}
```

---

## ✨ **ویژگی‌ها و قابلیت‌ها**

### 🏋️‍♂️ **بخش‌های اصلی وبسایت**
- **صفحه Home دینامیک**: بخش‌های hero، کارت‌های feature، testimonials با داده واقعی
- **مدیریت Services**: ایجاد، خواندن، بروزرسانی، حذف خدمات تناسب اندام
- **پروفایل‌های Team**: اطلاعات trainer با تخصص‌ها و لینک‌های social
- **سیستم Blog**: مدیریت مقاله با دسته‌بندی و جستجوی full-text
- **فرم‌های Contact**: تولید lead با اعلان‌های ایمیل
- **پلن‌های Pricing**: مدیریت عضویت با آماده‌سازی integration پرداخت
- **Gallery**: مدیریت عکس با دسته‌بندی و previewهای modal

### 🔧 **قابلیت‌های فنی**

#### **محتوای Database-Driven**
```javascript
// همه محتوا از طریق collections MongoDB مدیریت می‌شود
const services = await Service.find({ isActive: true });
const blogPosts = await Blog.find().sort({ createdAt: -1 }).limit(10);
const teamMembers = await Team.find().populate('specializations');
```

#### **طراحی RESTful API**
```javascript
// عملیات CRUD کامل برای همه resources
// GET    /api/services     - بازیابی همه services
// GET    /api/services/:id - دریافت service خاص
// POST   /api/services     - ایجاد service جدید
// PUT    /api/services/:id - بروزرسانی service موجود
// DELETE /api/services/:id - حذف service
```

#### **الگوهای پیشرفته React**
- **Custom Hooks**: data fetching، مدیریت فرم، state management API
- **Context API**: state global برای authentication کاربر و تنظیمات اپ
- **Error Boundaries**: مدیریت graceful خطا و feedback کاربر
- **Code Splitting**: lazy loading برای بهبود کارایی
- **Responsive Design**: integration Bootstrap با رویکرد mobile-first

#### **ویژگی‌های Production-Ready**
- **اعتبار سنجی ورودی**: اعتبار سنجی frontend و backend با schemaها
- **مدیریت خطا**: comprehensive error boundaries و messaging
- **بهینه‌سازی SEO**: meta tags، structured data، social sharing
- **کارایی**: تصاویر بهینه‌شده، lazy loading، استراتژی‌های caching
- **دسترسی‌پذیری**: ARIA labels، navigation keyboard، screen reader support

---

## 📁 **معماری کامل پروژه**

### **ساختار فولدر Backend**
```
fitmas-mern/
├── backend/
│   ├── config/
│   │   └── database.js          # تنظیمات اتصال MongoDB
│   ├── controllers/             # لایه business logic
│   │   ├── serviceController.js # عملیات CRUD services
│   │   ├── blogController.js    # منطق مدیریت blog
│   │   ├── teamController.js    # عملیات اعضای تیم
│   │   ├── pricingController.js # مدیریت پلن‌های قیمت‌گذاری
│   │   └── contactController.js # پردازش فرم contact
│   ├── models/                  # لایه داده - schemaهای MongoDB
│   │   ├── Service.js           # تعریف مدل Service
│   │   ├── Blog.js              # schema پست‌های blog
│   │   ├── Team.js              # مدل اعضای تیم
│   │   ├── Pricing.js           # schema پلن‌های قیمت‌گذاری
│   │   └── Contact.js           # مدل پیام‌های contact
│   ├── routes/                  # لایه routing API
│   │   ├── serviceRoutes.js     # endpointهای service
│   │   ├── blogRoutes.js        # مسیرهای API blog
│   │   ├── teamRoutes.js        # مسیرهای اعضای تیم
│   │   ├── pricingRoutes.js     # مسیرهای پلن‌های قیمت‌گذاری
│   │   └── contactRoutes.js     # مسیرهای فرم contact
│   ├── middleware/              # middlewareهای custom
│   ├── utils/                   # توابع helper
│   ├── .env                     # متغیرهای محیطی
│   ├── package.json            # وابستگی‌های backend
│   └── server.js               # نقطه ورود اپلیکیشن Express
```

### **ساختار فولدر Frontend**
```
fitmas-mern/
├── frontend/
│   ├── public/
│   │   ├── index.html           # قالب HTML
│   │   ├── assets/              # assetهای static (تصاویر، فونت‌ها)
│   │   └── favicon.ico          # favicon سایت
│   ├── src/
│   │   ├── components/          # کامپوننت‌های UI قابل استفاده مجدد
│   │   │   ├── Header/          # کامپوننت navigation
│   │   │   ├── Footer/          # footer سایت
│   │   │   ├── Preloader/       # انیمیشن loading
│   │   │   └── common/          # کامپوننت‌های shared
│   │   ├── pages/               # کامپوننت‌های page-level
│   │   │   ├── Home/            # کامپوننت homepage
│   │   │   ├── About/           # صفحه About
│   │   │   ├── Services/        # لیست services
│   │   │   ├── ServiceDetails/  # service انفرادی
│   │   │   ├── Team/            # لیست تیم
│   │   │   ├── TeamDetails/     # trainer انفرادی
│   │   │   ├── Blog/            # لیست blog
│   │   │   ├── BlogDetails/     # مقاله انفرادی
│   │   │   ├── Contact/         # فرم contact
│   │   │   ├── Pricing/         # پلن‌های قیمت‌گذاری
│   │   │   ├── Gallery/         # گالری تصاویر
│   │   │   └── Error/           # صفحه خطای 404
│   │   ├── services/            # توابع service API
│   │   │   └── api.js           # تنظیمات Axios
│   │   ├── hooks/               # custom React hooks
│   │   ├── contexts/            # React context providers
│   │   ├── utils/               # توابع utility
│   │   ├── App.js               # کامپوننت اپلیکیشن اصلی
│   │   ├── App.css              # استایل‌های global
│   │   ├── index.js             # نقطه ورود اپلیکیشن React
│   │   └── index.css            # importهای CSS base
│   ├── build/                   # خروجی build تولید
│   ├── package.json            # وابستگی‌های frontend
│   └── tailwind.config.js       # تنظیمات Tailwind
```

---

## 🚀 **راه‌اندازی محیط توسعه**

### **پیش‌نیازها Checklist**
- [x] **Node.js** (v18.0.0 یا بالاتر) - https://nodejs.org/
- [x] **MongoDB** (نصب محلی یا حساب MongoDB Atlas)
- [x] **Git** - https://git-scm.com/
- [x] **Visual Studio Code** (IDE پیشنهادی)
- [x] **مرورگر وب مدرن** (Chrome, Firefox, Safari, Edge)

### **راهنمای شروع سریع**

#### **1. Clone و Navigate**
```bash
# Clone کردن repository
git clone https://github.com/yourusername/fitmas-mern.git
cd fitmas-mern

# بررسی ساختار پروژه
ls -la
# باید ببینید: backend/, frontend/, README.md, deploy-test.js
```

#### **2. راه‌اندازی Backend**
```bash
# نصب وابستگی‌های backend
cd backend
npm install

# تنظیم متغیرهای محیطی
cp .env.example .env
# ویرایش فایل .env با connection string MongoDB خود

# راه‌اندازی سرور backend
npm run dev
# Backend روی http://localhost:5000 شروع می‌شود
```

#### **3. راه‌اندازی Frontend**
```bash
# باز کردن terminal جدید
cd frontend
npm install

# راه‌اندازی سرور توسعه React
npm start
# Frontend روی http://localhost:3000 شروع می‌شود
```

#### **4. بررسی پایگاه داده**
```bash
# اطمینان از اجرای MongoDB
mongosh --eval "db.runCommand('ping')"

# یا استفاده از MongoDB Compass برای بررسی اتصال
# Connection string: mongodb://localhost:27017/fitmas_db
```

### **تنظیمات محیطی**

#### **متغیرهای محیطی Backend (.env)**
```env
# تنظیمات سرور
PORT=5000
NODE_ENV=development

# تنظیمات پایگاه داده
MONGODB_URI=mongodb://localhost:27017/fitmas_db

# تنظیمات ایمیل (اختیاری)
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# JWT Secret (برای authentication آینده)
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# تنظیمات CORS
FRONTEND_URL=http://localhost:3000
```

#### **متغیرهای محیطی Frontend**
```env
# تنظیمات API
REACT_APP_API_URL=http://localhost:5000/api

# تنظیمات اپلیکیشن
REACT_APP_NAME=Fitmas Fitness
REACT_APP_VERSION=1.0.0
```

### **Workflow توسعه**

#### **اجرا کردن اپلیکیشن**
```bash
# راه‌اندازی هر دو سرور همزمان (نیاز به terminalهای متعدد)
# Terminal 1: Backend
cd backend && npm run dev

# Terminal 2: Frontend
cd frontend && npm start

# یا استفاده از concurrently (اگر تنظیم شده باشد)
npm run dev:full
```

#### **Build برای تولید**
```bash
# Build کردن frontend
cd frontend
npm run build

# Build بهینه‌سازی اجرا می‌کند و فایل‌های production-ready را در فولدر build/ ایجاد می‌کند

# Backend آماده deploy است (server.js نقطه ورود)
```

---

## 🌐 **راهنمای معماری Backend**

### **تنظیمات Express.js Server**

#### **فایل Server اصلی (server.js)**
```javascript
const express = require('express');
const dotenv = require('dotenv');
const cors = require('cors');
const path = require('path');
const helmet = require('helmet');
const rateLimit = require('express-rate-limit');
const connectDB = require('./config/database');

// Import مسیرها
const serviceRoutes = require('./routes/serviceRoutes');
const blogRoutes = require('./routes/blogRoutes');
const teamRoutes = require('./routes/teamRoutes');
const pricingRoutes = require('./routes/pricingRoutes');
const contactRoutes = require('./routes/contactRoutes');
const authRoutes = require('./routes/auth');
const uploadRoutes = require('./routes/upload');
const adminRoutes = require('./routes/admin');
const contentRoutes = require('./routes/content');

// بارگذاری متغیرهای محیطی
dotenv.config();

// اتصال به پایگاه داده
connectDB();

// مقداردهی اولیه اپ Express
const app = express();

// Middleware امنیتی
app.use(helmet({
  crossOriginResourcePolicy: { policy: "cross-origin" }
}));

// محدودسازی نرخ
const limiter = rateLimit({
  windowMs: 15 * 60 * 1000, // 15 دقیقه
  max: 100, // محدودیت هر IP به 100 درخواست در windowMs
  message: {
    success: false,
    message: 'درخواست‌های خیلی زیاد از این IP، لطفا بعدا تلاش کنید.'
  }
});
app.use('/api/', limiter);

// مسیرهای auth - بدون محدودسازی نرخ برای login
const authLimiter = rateLimit({
  windowMs: 15 * 60 * 1000, // 15 دقیقه
  max: 5, // محدودیت هر IP به 5 تلاش login در windowMs
  message: {
    success: false,
    message: 'تلاش‌های login خیلی زیاد، لطفا بعدا تلاش کنید.'
  }
});

// Middleware
app.use(cors());
app.use(express.json({ limit: '10mb' }));
app.use(express.urlencoded({ extended: true }));

// سرویس‌دهی فایل‌های static از فولدر uploads
app.use('/uploads', express.static(path.join(__dirname, 'uploads')));

// مسیرها
app.use('/api/auth/login', authLimiter);
app.use('/api/auth/register', authLimiter);
app.use('/api/auth', authRoutes);
app.use('/api/upload', uploadRoutes);
app.use('/api/admin', adminRoutes);
app.use('/api/content', contentRoutes);
app.use('/api/services', serviceRoutes);
app.use('/api/blogs', blogRoutes);
app.use('/api/team', teamRoutes);
app.use('/api/pricing', pricingRoutes);
app.use('/api/contact', contactRoutes);

// Health check endpoint
app.get('/api/health', (req, res) => {
  res.json({
    success: true,
    message: 'API Fitmas با موفقیت اجرا می‌شود',
    timestamp: new Date().toISOString()
  });
});

// مسیر اصلی
app.get('/', (req, res) => {
  res.json({
    message: 'به API تناسب اندام Fitmas خوش آمدید',
    version: '1.0.0'
  });
});

// Middleware مدیریت خطا
app.use((err, req, res, next) => {
  console.error(err.stack);
  res.status(500).json({
    success: false,
    message: process.env.NODE_ENV === 'production' ? 'مشکلی پیش آمده!' : err.message
  });
});

// Handler 404
app.use('*', (req, res) => {
  res.status(404).json({
    success: false,
    message: 'Endpoint API یافت نشد'
  });
});

const PORT = process.env.PORT || 5000;

app.listen(PORT, () => {
  console.log(`🚀 API Server Fitmas روی پورت ${PORT} اجرا می‌شود`);
  console.log(`🌍 محیط: ${process.env.NODE_ENV || 'development'}`);
});

// مدیریت unhandled promise rejections
process.on('unhandledRejection', (err, promise) => {
  console.log(`خطا: ${err.message}`);
  // متوقف کردن سرور و خروج از فرآیند
  process.exit(1);
});

module.exports = app;
```

### **اتصال پایگاه داده (config/database.js)**
```javascript
const mongoose = require('mongoose');

const connectDB = async () => {
  try {
    const options = {
      useNewUrlParser: true,
      useUnifiedTopology: true,
      maxPoolSize: 10,
      serverSelectionTimeoutMS: 5000,
      socketTimeoutMS: 45000,
    };

    const connection = await mongoose.connect(process.env.MONGODB_URI, options);

    console.log(`✅ MongoDB متصل شد: ${connection.connection.host}`);
    console.log(`📊 پایگاه داده: ${connection.connection.name}`);

    // مدیریت eventهای اتصال
    mongoose.connection.on('error', (err) => {
      console.error('❌ خطای اتصال MongoDB:', err);
    });

    mongoose.connection.on('disconnected', () => {
      console.warn('⚠️  MongoDB قطع شد');
    });

  } catch (error) {
    console.error('❌ اتصال پایگاه داده ناموفق:', error.message);
    process.exit(1);
  }
};

module.exports = connectDB;
```

---

## 🔧 **Custom React Hooks و تبدیل‌های jQuery**

### **چرا Custom Hooks؟**

به عنوان یک توسعه‌دهنده full-stack، زود متوجه شدم که قالب سنگین jQuery نیاز به بازسازی معماری کامل دارد. به جای کپی کردن کد jQuery در کامپوننت‌های React، تصمیم گرفتم custom hooks بسازم که رفتارهای interactive را به شیوه React-native کپسوله کنند. این رویکرد مزایای زیر را فراهم می‌کند:

- **قابل استفاده مجدد**: Hooks می‌توانند در کامپوننت‌های متعدد به اشتراک گذاشته شوند
- **قابل تست**: منطق به راحتی در isolation تست می‌شود
- **کارایی**: ادغام بهتر با چرخه rendering React
- **قابل نگهداری**: منطق متمرکز که اشکال‌زدایی آسان‌تری دارد

### **Custom Hooks که ساختم**

#### **1. Hook useStickyNavigation**
```javascript
// src/hooks/useStickyNavigation.js
import { useEffect, useState } from 'react';

const useStickyNavigation = () => {
  const [isScrolled, setIsScrolled] = useState(false);

  useEffect(() => {
    const handleScroll = () => {
      const scrollPosition = window.scrollY;
      const threshold = 100; // pixels از بالا

      if (scrollPosition > threshold && !isScrolled) {
        setIsScrolled(true);
      } else if (scrollPosition <= threshold && isScrolled) {
        setIsScrolled(false);
      }
    };

    window.addEventListener('scroll', handleScroll, { passive: true });
    return () => window.removeEventListener('scroll', handleScroll);
  }, [isScrolled]);

  return isScrolled;
};

// استفاده در کامپوننت Header
const Header = () => {
  const isScrolled = useStickyNavigation();

  return (
    <header className={`nav-header ${isScrolled ? 'sticky-active' : ''}`}>
      {/* محتوای Header */}
    </header>
  );
};
```
**چه چیزی جایگزین کرد**: کد sticky navigation jQuery اصلی که کلاس‌های CSS را به صورت دستی اضافه/حذف می‌کرد.

#### **2. Hook useCounterUp**
```javascript
// src/hooks/useCounterUp.js
import { useEffect, useRef, useState } from 'react';

const useCounterUp = (endValue, duration = 2000, trigger = true) => {
  const [count, setCount] = useState(0);
  const countRef = useRef(null);
  const [isVisible, setIsVisible] = useState(false);

  useEffect(() => {
    if (!trigger) return;

    const observer = new IntersectionObserver(
      ([entry]) => {
        if (entry.isIntersecting && !isVisible) {
          setIsVisible(true);
        }
      },
      { threshold: 0.3 }
    );

    if (countRef.current) {
      observer.observe(countRef.current);
    }

    return () => observer.disconnect();
  }, [trigger, isVisible]);

  useEffect(() => {
    if (!isVisible) return;

    let startTime;
    const startValue = 0;

    const animate = (currentTime) => {
      if (!startTime) startTime = currentTime;
      const progress = Math.min((currentTime - startTime) / duration, 1);

      // تابع easing برای انیمیشن smooth
      const easeOutQuart = 1 - Math.pow(1 - progress, 4);
      const currentValue = Math.floor(startValue + (endValue - startValue) * easeOutQuart);

      setCount(currentValue);

      if (progress < 1) {
        requestAnimationFrame(animate);
      }
    };

    requestAnimationFrame(animate);
  }, [isVisible, endValue, duration]);

  return [count, countRef];
};

// استفاده در کامپوننت Counter
const CounterCard = ({ endValue, title }) => {
  const [count, countRef] = useCounterUp(endValue);

  return (
    <div className="counter-card" ref={countRef}>
      <h2 className="counter-card_number">
        <span className="counter-number">{count}</span>
        {title.includes('K') ? 'K' : ''}
      </h2>
      <p className="counter-card_text">{title}</p>
    </div>
  );
};
```
**چه چیزی جایگزین کرد**: پلاگین‌های counter animation jQuery که کنترل و debug دشواری داشتند.

#### **3. Hook useShapeMockup**
```javascript
// src/hooks/useShapeMockup.js
import { useEffect } from 'react';

const useShapeMockup = (selector, animation = 'movingX') => {
  useEffect(() => {
    const elements = document.querySelectorAll(selector);

    elements.forEach(element => {
      // اضافه کردن کلاس انیمیشن
      element.classList.add(animation);

      // تنظیم تأخیر انیمیشن تصادفی برای تنوع
      const delay = Math.random() * 2; // 0-2 ثانیه
      element.style.animationDelay = `${delay}s`;

      // تعامل اختیاری mouse
      const handleMouseEnter = () => {
        element.style.animationPlayState = 'paused';
      };

      const handleMouseLeave = () => {
        element.style.animationPlayState = 'running';
      };

      element.addEventListener('mouseenter', handleMouseEnter);
      element.addEventListener('mouseleave', handleMouseLeave);

      return () => {
        element.removeEventListener('mouseenter', handleMouseEnter);
        element.removeEventListener('mouseleave', handleMouseLeave);
      };
    });
  }, [selector, animation]);
};

// استفاده در کامپوننت‌ها
const HeroSection = () => {
  useShapeMockup('.hero-shape1', 'movingX');

  return (
    <div className="hero-wrapper">
      <div className="hero-shape1 shape-mockup">
        <img src="/assets/img/hero/hero_shape_1.png" alt="shape" />
      </div>
      {/* محتوای Hero */}
    </div>
  );
};
```
**چه چیزی جایگزین کرد**: کد انیمیشن jQuery که transformهای CSS را به صورت دستی دستکاری می‌کرد.

#### **4. Hook useCardHoverEffects**
```javascript
// src/hooks/useCardHoverEffects.js
import { useEffect } from 'react';

const useCardHoverEffects = (cardSelector = '.service-card, .team-card, .blog-card') => {
  useEffect(() => {
    const cards = document.querySelectorAll(cardSelector);

    const handleMouseEnter = (e) => {
      const card = e.currentTarget;
      const image = card.querySelector('img');

      // اضافه کردن افکت lift
      card.style.transform = 'translateY(-10px)';
      card.style.boxShadow = '0 20px 40px rgba(0,0,0,0.15)';

      // Scale کردن تصویر اگر موجود باشد
      if (image) {
        image.style.transform = 'scale(1.05)';
      }
    };

    const handleMouseLeave = (e) => {
      const card = e.currentTarget;
      const image = card.querySelector('img');

      // ریست کردن افکت‌ها
      card.style.transform = 'translateY(0)';
      card.style.boxShadow = '0 10px 30px rgba(0,0,0,0.1)';

      if (image) {
        image.style.transform = 'scale(1)';
      }
    };

    cards.forEach(card => {
      card.addEventListener('mouseenter', handleMouseEnter);
      card.addEventListener('mouseleave', handleMouseLeave);
    });

    return () => {
      cards.forEach(card => {
        card.removeEventListener('mouseenter', handleMouseEnter);
        card.removeEventListener('mouseleave', handleMouseLeave);
      });
    };
  }, [cardSelector]);
};

// استفاده در هر کامپوننتی با کارت‌ها
const ServicesPage = () => {
  useCardHoverEffects('.service-card');

  return (
    <div className="services-grid">
      {/* کارت‌های service */}
    </div>
  );
};
```
**چه چیزی جایگزین کرد**: event handlerهای hover jQuery پراکنده در codebase.

#### **5. Hook useProgressBar**
```javascript
// src/hooks/useProgressBar.js
import { useEffect, useState } from 'react';

const useProgressBar = (selector = '.progress-bar') => {
  const [scrollProgress, setScrollProgress] = useState(0);

  useEffect(() => {
    const updateProgressBar = () => {
      const scrollTop = window.scrollY;
      const docHeight = document.documentElement.scrollHeight - window.innerHeight;
      const scrollPercent = (scrollTop / docHeight) * 100;

      setScrollProgress(scrollPercent);
    };

    window.addEventListener('scroll', updateProgressBar, { passive: true });
    updateProgressBar(); // فراخوانی اولیه

    return () => window.removeEventListener('scroll', updateProgressBar);
  }, []);

  useEffect(() => {
    const progressBars = document.querySelectorAll(selector);
    progressBars.forEach(bar => {
      bar.style.width = `${scrollProgress}%`;
    });
  }, [scrollProgress, selector]);

  return scrollProgress;
};

// استفاده در کامپوننت ScrollToTop
const ScrollToTop = () => {
  const progress = useProgressBar('.progress-circle path');

  return (
    <div className="scroll-top">
      <svg className="progress-circle svg-content" width="100%" height="100%" viewBox="-1 -1 102 102">
        <path
          d="M50,1 a49,49 0 0,1 0,98 a49,49 0 0,1 0,-98"
          style={{
            strokeDasharray: '307.919, 307.919',
            strokeDashoffset: `${307.919 - (progress / 100) * 307.919}`
          }}
        />
      </svg>
    </div>
  );
};
```
**چه چیزی جایگزین کرد**: event listenerهای scroll jQuery برای progress indicatorها.

#### **6. Hook useOnePageNav**
```javascript
// src/hooks/useOnePageNav.js
import { useEffect } from 'react';

const useOnePageNav = (navSelector = '.main-menu a') => {
  useEffect(() => {
    const navLinks = document.querySelectorAll(navSelector);

    const handleNavClick = (e) => {
      const target = e.currentTarget.getAttribute('href');

      // بررسی anchor link (با # شروع می‌شود)
      if (target && target.startsWith('#')) {
        e.preventDefault();

        const targetElement = document.querySelector(target);
        if (targetElement) {
          const headerOffset = 100; // حساب کردن header ثابت
          const elementPosition = targetElement.offsetTop;
          const offsetPosition = elementPosition - headerOffset;

          window.scrollTo({
            top: offsetPosition,
            behavior: 'smooth'
          });
        }
      }
    };

    navLinks.forEach(link => {
      link.addEventListener('click', handleNavClick);
    });

    return () => {
      navLinks.forEach(link => {
        link.removeEventListener('click', handleNavClick);
      });
    };
  }, [navSelector]);
};

// استفاده در کامپوننت Header
const Header = () => {
  useOnePageNav('.main-menu a[href^="#"]');

  return (
    <nav className="main-menu">
      <ul>
        <li><a href="#home">Home</a></li>
        <li><a href="#services">Services</a></li>
        <li><a href="#contact">Contact</a></li>
      </ul>
    </nav>
  );
};
```
**چه چیزی جایگزین کرد**: پلاگین‌های smooth scrolling jQuery.

#### **7. Hook useBackgroundImage**
```javascript
// src/hooks/useBackgroundImage.js
import { useEffect } from 'react';

const useBackgroundImage = (selector = '[data-bg-src]') => {
  useEffect(() => {
    const elements = document.querySelectorAll(selector);

    elements.forEach(element => {
      const bgSrc = element.getAttribute('data-bg-src');
      if (bgSrc) {
        element.style.backgroundImage = `url(${bgSrc})`;
        element.style.backgroundSize = 'cover';
        element.style.backgroundPosition = 'center center';
        element.style.backgroundRepeat = 'no-repeat';
      }
    });
  }, [selector]);
};

// استفاده در هر کامپوننتی
const HeroSection = () => {
  useBackgroundImage('.hero-wrapper[data-bg-src]');

  return (
    <div className="hero-wrapper" data-bg-src="/assets/img/hero/hero_bg_1_1.png">
      {/* محتوای Hero */}
    </div>
  );
};
```
**چه چیزی جایگزین کرد**: کد jQuery که background imageها را به صورت دینامیک تنظیم می‌کرد.

#### **8. Hook useAnimationAttributes**
```javascript
// src/hooks/useAnimationAttributes.js
import { useEffect } from 'react';

const useAnimationAttributes = () => {
  useEffect(() => {
    const observerOptions = {
      threshold: 0.1,
      rootMargin: '0px 0px -50px 0px'
    };

    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          const element = entry.target;
          const delay = element.getAttribute('data-ani-delay') || '0s';
          const duration = element.getAttribute('data-ani-duration') || '1s';

          // اعمال استایل‌های انیمیشن
          element.style.animationDelay = delay;
          element.style.animationDuration = duration;
          element.style.animationFillMode = 'both';

          // اضافه کردن کلاس انیمیشن
          element.classList.add('slideinup');

          // متوقف کردن observe پس از انیمیشن
          observer.unobserve(element);
        }
      });
    }, observerOptions);

    // observe کردن عناصر با attributeهای انیمیشن
    const animatedElements = document.querySelectorAll('[data-ani]');
    animatedElements.forEach(el => observer.observe(el));

    return () => observer.disconnect();
  }, []);
};

// استفاده در App.js یا هر کامپوننتی
const App = () => {
  useAnimationAttributes();

  return (
    <div>
      <h1 data-ani="slideinup" data-ani-delay="0.2s">Heading انیمیشن‌دار</h1>
      <p data-ani="slideinleft" data-ani-delay="0.4s">paragraph انیمیشن‌دار</p>
    </div>
  );
};
```
**چه چیزی جایگزین کرد**: کتابخانه‌های انیمیشن jQuery که CSS animations را روی scroll trigger می‌کردند.

### **کامپوننت ScrollToTop (تبدیل jQuery به React)**
```javascript
// src/components/ScrollToTop/ScrollToTop.jsx
import React, { useEffect, useState } from 'react';
import './ScrollToTop.css';

const ScrollToTop = () => {
  const [isVisible, setIsVisible] = useState(false);
  const [scrollProgress, setScrollProgress] = useState(0);

  useEffect(() => {
    const toggleVisibility = () => {
      const scrolled = document.documentElement.scrollTop;
      setIsVisible(scrolled > 300);

      // محاسبه progress scroll
      const scrollTop = window.scrollY;
      const docHeight = document.documentElement.scrollHeight - window.innerHeight;
      const scrollPercent = (scrollTop / docHeight) * 100;
      setScrollProgress(scrollPercent);
    };

    const scrollToTop = () => {
      window.scrollTo({
        top: 0,
        behavior: 'smooth'
      });
    };

    window.addEventListener('scroll', toggleVisibility, { passive: true });

    // اضافه کردن event handler به دکمه scroll
    const scrollBtn = document.querySelector('.scroll-top');
    if (scrollBtn) {
      scrollBtn.addEventListener('click', scrollToTop);
    }

    return () => {
      window.removeEventListener('scroll', toggleVisibility);
      if (scrollBtn) {
        scrollBtn.removeEventListener('click', scrollToTop);
      }
    };
  }, []);

  // محاسبه طول path برای progress circle
  const pathLength = 307.919;
  const dashOffset = pathLength - (scrollProgress / 100) * pathLength;

  return (
    <div className={`scroll-top ${isVisible ? 'show' : ''}`}>
      <svg className="progress-circle svg-content" width="100%" height="100%" viewBox="-1 -1 102 102">
        <path
          d="M50,1 a49,49 0 0,1 0,98 a49,49 0 0,1 0,-98"
          style={{
            transition: 'stroke-dashoffset 10ms linear 0s',
            strokeDasharray: `${pathLength}, ${pathLength}`,
            strokeDashoffset: dashOffset
          }}
        />
      </svg>
    </div>
  );
};

export default ScrollToTop;
```
**چه چیزی جایگزین کرد**: عملکرد scroll-to-top jQuery با progress indicator.

### **کامپوننت Mobile Menu (تبدیل کامل jQuery به React)**
```javascript
// src/components/Header/Header.jsx (excerpt)
const Header = () => {
  const [isMobileMenuOpen, setIsMobileMenuOpen] = useState(false);

  const toggleMobileMenu = () => {
    setIsMobileMenuOpen(!isMobileMenuOpen);
  };

  const closeAllMenus = () => {
    setIsMobileMenuOpen(false);
  };

  return (
    <>
      {/* دکمه Mobile Menu */}
      <div className="navbar-right d-inline-flex d-lg-none">
        <button
          type="button"
          className="menu-toggle icon-btn"
          onClick={toggleMobileMenu}
          aria-label="Toggle mobile menu"
        >
          <i className="far fa-bars"></i>
        </button>
      </div>

      {/* Mobile Menu Overlay */}
      <div className={`mobile-menu-wrapper ${isMobileMenuOpen ? 'body-visible' : ''}`}>
        <div className="mobile-menu-area text-center">
          <button className="menu-toggle" onClick={toggleMobileMenu}>
            <i className="fal fa-times"></i>
          </button>
          <div className="mobile-logo">
            <Link to="/" onClick={closeAllMenus}>
              <img src="/assets/img/logo.svg" alt="Fitmas" />
            </Link>
          </div>
          <div className="mobile-menu">
            <ul>
              <li>
                <Link to="/" onClick={closeAllMenus}>Home</Link>
              </li>
              <li>
                <Link to="/about" onClick={closeAllMenus}>About</Link>
              </li>
              <li>
                <Link to="/services" onClick={closeAllMenus}>Services</Link>
              </li>
              <li>
                <Link to="/contact" onClick={closeAllMenus}>Contact</Link>
              </li>
            </ul>
          </div>
        </div>
      </div>
    </>
  );
};
```
**چه چیزی جایگزین کرد**: منطق پیچیده toggle mobile menu jQuery با مدیریت state مناسب React.

### **چرا این رویکرد مهم است؟**

از تجربه‌ام به عنوان توسعه‌دهنده full-stack، تبدیل jQuery به React فقط جایگزینی syntax نیست—درک مجدد نحوه عملکرد interactivity در معماری component-based است:

1. **کارایی**: Virtual DOM React و hooks کارایی بهتری نسبت به دستکاری DOM jQuery دارند
2. **قابل نگهداری**: منطق متمرکز در custom hooks اشکال‌زدایی آسان‌تری دارد
3. **قابل استفاده مجدد**: Hooks بدون duplication در کامپوننت‌ها به اشتراک گذاشته می‌شوند
4. **تست**: React hooks بسیار آسان‌تر از event handlerهای jQuery تست می‌شوند
5. **الگوهای مدرن**: از ویژگی‌ها و بهترین تمرین‌های React مدرن استفاده می‌کند

این رویکرد تبدیل اطمینان می‌دهد که وبسایت ظاهر اصلی خود را حفظ کند و تجربه کاربری را در حالی که از مزایای معماری مدرن React بهره‌مند شود.

---

## ⚛️ **جزئیات عمیق React Frontend**

### **App.js - کامپوننت اپلیکیشن اصلی**
```javascript
import React, { useState, useEffect } from 'react';
import { BrowserRouter as Router, Routes, Route, Navigate } from 'react-router-dom';
import { Toaster } from 'react-hot-toast';
import './App.css';

// کامپوننت‌های Admin
import { AuthProvider, useAuth } from './admin/context/AuthContext';
import AdminLayout from './admin/components/AdminLayout';
import AdminLogin from './admin/pages/AdminLogin';
import Dashboard from './admin/pages/Dashboard';
import BlogList from './admin/pages/BlogList';
import AddBlog from './admin/pages/AddBlog';
import EditBlog from './admin/pages/EditBlog';
import ContentList from './admin/pages/ContentList';

// کامپوننت‌های Public
import Header from './components/Header/Header';
import Footer from './components/Footer/Footer';
import Preloader from './components/Preloader/Preloader';

// صفحات Public
import Home from './pages/Home/Home';
import About from './pages/About/About';
import Services from './pages/Services/Services';
import ServiceDetails from './pages/ServiceDetails/ServiceDetails';
import Team from './pages/Team/Team';
import TeamDetails from './pages/TeamDetails/TeamDetails';
import Blog from './pages/Blog/Blog';
import BlogDetails from './pages/BlogDetails/BlogDetails';
import Contact from './pages/Contact/Contact';
import Pricing from './pages/Pricing/Pricing';
import Gallery from './pages/Gallery/Gallery';
import ErrorPage from './pages/Error/ErrorPage';

// کامپوننت Protected Route
const ProtectedRoute = ({ children }) => {
  const { isAuthenticated, loading } = useAuth();

  if (loading) {
    return <div className="loading-spinner" style={{
      width: '40px',
      height: '40px',
      border: '4px solid #f3f3f3',
      borderTop: '4px solid #007bff',
      borderRadius: '50%',
      animation: 'spin 1s linear infinite',
      margin: '100px auto'
    }}></div>;
  }

  return isAuthenticated ? children : <Navigate to="/admin/login" />;
};

function AppContent() {
  const [isLoading, setIsLoading] = useState(true);

  useEffect(() => {
    console.log('App loaded, showing preloader');
    // شبیه‌سازی زمان loading اولیه (یا برای instant loading حذف کنید)
    const timer = setTimeout(() => {
      console.log('Preloader timeout reached, hiding preloader');
      setIsLoading(false);
    }, 1000); // 1 ثانیه preloader

    return () => clearTimeout(timer);
  }, []);

  if (isLoading) {
    console.log('Rendering preloader component');
    return <Preloader />;
  }

  return (
    <div className="App">
      <Routes>
        {/* مسیرهای Admin */}
        <Route path="/admin/login" element={<AdminLogin />} />
        <Route path="/admin" element={
          <ProtectedRoute>
            <AdminLayout>
              <Dashboard />
            </AdminLayout>
          </ProtectedRoute>
        } />
        <Route path="/admin/blogs" element={
          <ProtectedRoute>
            <AdminLayout>
              <BlogList />
            </AdminLayout>
          </ProtectedRoute>
        } />
        <Route path="/admin/blogs/add" element={
          <ProtectedRoute>
            <AdminLayout>
              <AddBlog />
            </AdminLayout>
          </ProtectedRoute>
        } />
        <Route path="/admin/blogs/edit/:id" element={
          <ProtectedRoute>
            <AdminLayout>
              <EditBlog />
            </AdminLayout>
          </ProtectedRoute>
        } />
        <Route path="/admin/content" element={
          <ProtectedRoute>
            <AdminLayout>
              <ContentList />
            </AdminLayout>
          </ProtectedRoute>
        } />

        {/* مسیرهای Public */}
        <Route path="/*" element={
          <>
            <Header />
            <main>
              <Routes>
                {/* صفحات Home */}
                <Route path="/" element={<Home />} />
                <Route path="/home-2" element={<Home variant="home-2" />} />
                <Route path="/home-3" element={<Home variant="home-3" />} />

                {/* صفحات اصلی */}
                <Route path="/about" element={<About />} />
                <Route path="/services" element={<Services />} />
                <Route path="/services/:slug" element={<ServiceDetails />} />
                <Route path="/team" element={<Team />} />
                <Route path="/team/:slug" element={<TeamDetails />} />
                <Route path="/blog" element={<Blog />} />
                <Route path="/blog/:slug" element={<BlogDetails />} />
                <Route path="/contact" element={<Contact />} />
                <Route path="/pricing" element={<Pricing />} />

                {/* صفحات Gallery */}
                <Route path="/gallery" element={<Gallery />} />
                <Route path="/gallery-2" element={<Gallery variant="gallery-2" />} />

                {/* صفحات Projects (برای حالا از Gallery استفاده کنید) */}
                <Route path="/projects" element={<Gallery />} />
                <Route path="/projects/:slug" element={<Gallery />} />

                {/* صفحه خطای 404 */}
                <Route path="*" element={<ErrorPage />} />
              </Routes>
            </main>
            <Footer />
          </>
        } />
      </Routes>
    </div>
  );
}

function App() {
  return (
    <AuthProvider>
      <Router>
        <Toaster
          position="top-right"
          toastOptions={{
            duration: 4000,
            style: {
              background: '#363636',
              color: '#fff',
            },
            success: {
              duration: 3000,
              iconTheme: {
                primary: '#10B981',
                secondary: '#fff',
              },
            },
            error: {
              duration: 5000,
              iconTheme: {
                primary: '#EF4444',
                secondary: '#fff',
              },
            },
          }}
        />
        <AppContent />
      </Router>
    </AuthProvider>
  );
}

export default App;
```

### **پیاده‌سازی Custom Hooks**

#### **Hook useFetchData (توصیه شده)**
```javascript
// src/hooks/useFetchData.js
import { useState, useEffect } from 'react';
import axios from '../services/api';

const useFetchData = (endpoint, initialData = []) => {
  const [data, setData] = useState(initialData);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState(null);

  useEffect(() => {
    const fetchData = async () => {
      try {
        setLoading(true);
        const response = await axios.get(endpoint);
        setData(response.data.data || initialData);
        setError(null);
      } catch (err) {
        console.error('Error fetching data:', err);
        setError(err.message);
      } finally {
        setLoading(false);
      }
    };

    if (endpoint) {
      fetchData();
    }
  }, [endpoint]);

  const refetch = async () => {
    // متد برای refetch دستی داده‌ها
    if (endpoint) {
      try {
        const response = await axios.get(endpoint);
        setData(response.data.data || initialData);
        setError(null);
      } catch (err) {
        setError(err.message);
      }
    }
  };

  return { data, loading, error, refetch };
};

export default useFetchData;
```

#### **کامپوننت Services با Hook**
```javascript
// src/pages/Services/Services.jsx - بروزرسانی برای استفاده از custom hook
import React from 'react';
import useFetchData from '../../hooks/useFetchData';

const Services = () => {
  const { data: services, loading, error } = useFetchData('/services');

  if (loading) {
    return (
      <div className="space">
        <div className="container text-center">
          <div className="loading-spinner"></div>
          <p>در حال بارگذاری services...</p>
        </div>
      </div>
    );
  }

  if (error) {
    return (
      <div className="space">
        <div className="container text-center">
          <div className="alert alert-danger">
            خطا در بارگذاری services: {error}
          </div>
        </div>
      </div>
    );
  }

  return (
    <div className="services-page">
      <div className="breadcumb-wrapper" style={{
        backgroundImage: "url('/assets/img/bg/breadcrumb-bg.png')"
      }}>
        <div className="container">
          <div className="row">
            <div className="col-lg-12">
              <div className="breadcumb-content">
                <h1 className="breadcumb-title">خدمات ما</h1>
                <ul className="breadcumb-menu">
                  <li><a href="/">خانه</a></li>
                  <li className="active">خدمات</li>
                </ul>
              </div>
            </div>
          </div>
        </div>
      </div>

      <div className="space">
        <div className="container">
          <div className="title-area text-center">
            <span className="sub-title">خدمات ما</span>
            <h2 className="sec-title">آنچه ارائه می‌دهیم</h2>
            <p className="sec-text">
              خدمات حرفه‌ای تناسب اندام متناسب با اهداف شما
            </p>
          </div>

          {services.length > 0 ? (
            <div className="row">
              {services.map((service) => (
                <div key={service._id} className="col-xl-4 col-md-6">
                  <div className="service-card style2">
                    <div className="service-card_icon">
                      <img
                        src={`/assets/img/icon/${service.icon}`}
                        alt={service.title}
                        onError={(e) => {
                          e.target.src = '/assets/img/icon/default-icon.svg';
                        }}
                      />
                    </div>
                    <div className="service-card_content">
                      <h4 className="service-card_title h5">
                        <a href={`/services/${service.slug}`}>
                          {service.title}
                        </a>
                      </h4>
                      <p className="service-card_text">{service.description}</p>
                      {service.features && service.features.length > 0 && (
                        <ul className="service-highlights">
                          {service.features.slice(0, 3).map((feature, idx) => (
                            <li key={idx}>✓ {feature}</li>
                          ))}
                        </ul>
                      )}
                      <a
                        href={`/services/${service.slug}`}
                        className="link-btn"
                        tabIndex="0"
                      >
                        بیشتر بخوانید <i className="fas fa-arrow-right"></i>
                      </a>
                    </div>
                  </div>
                </div>
              ))}
            </div>
          ) : (
            <div className="text-center">
              <div className="empty-state">
                <i className="fas fa-concierge-bell fa-3x text-muted mb-3"></i>
                <h3>سرویسی در دسترس نیست</h3>
                <p>در حال بروزرسانی خدمات هستیم. لطفا بعدا بررسی کنید!</p>
              </div>
            </div>
          )}
        </div>
      </div>
    </div>
  );
};

export default Services;
```

---

## 🗄️ **طراحی پایگاه داده و Models**

### **فلسفه طراحی Schema MongoDB**

#### **مدل Service - Core Content Type**
```javascript
// models/Service.js
const mongoose = require('mongoose');

const serviceSchema = new mongoose.Schema({
  // اطلاعات پایه
  title: {
    type: String,
    required: [true, 'عنوان service الزامی است'],
    trim: true,
    maxlength: [100, 'عنوان نمی‌تواند بیش از 100 کاراکتر باشد']
  },

  description: {
    type: String,
    required: [true, 'توضیحات service الزامی است'],
    maxlength: [500, 'توضیحات نمی‌تواند بیش از 500 کاراکتر باشد']
  },

  // SEO و Routing
  slug: {
    type: String,
    required: true,
    unique: true,
    lowercase: true,
    trim: true
  },

  // عناصر بصری
  icon: {
    type: String,
    required: true,
    enum: ['dumbbell', 'running', 'yoga', 'nutrition', 'cardio', 'strength']
  },

  image: {
    type: String, // URL یا مسیر فایل
    default: null
  },

  // جزئیات محتوا
  features: [{
    type: String,
    trim: true,
    maxlength: [200, 'توضیحات feature خیلی طولانی است']
  }],

  category: {
    type: String,
    enum: ['fitness', 'nutrition', 'wellness', 'training'],
    default: 'fitness'
  },

  duration: {
    type: Number, // به دقیقه
    min: [15, 'مدت زمان باید حداقل 15 دقیقه باشد'],
    max: [180, 'مدت زمان نمی‌تواند بیش از 3 ساعت باشد']
  },

  difficulty: {
    type: String,
    enum: ['beginner', 'intermediate', 'advanced'],
    default: 'beginner'
  },

  // قیمت‌گذاری (اختیاری)
  price: {
    amount: Number,
    currency: { type: String, default: 'USD' },
    period: { type: String, enum: ['session', 'month'], default: 'session' }
  },

  // کنترل‌های Publishing
  isActive: {
    type: Boolean,
    default: true
  },

  isPopular: {
    type: Boolean,
    default: false
  },

  displayOrder: {
    type: Number,
    default: 0
  }

}, {
  timestamps: true, // اضافه کردن createdAt و updatedAt

  // Virtualها
  toJSON: { virtuals: true },
  toObject: { virtuals: true }
});

// ایندکس‌ها برای کارایی
serviceSchema.index({ slug: 1 });
serviceSchema.index({ isActive: 1 });
serviceSchema.index({ category: 1 });

// Virtual برای نمایش قیمت کامل
serviceSchema.virtual('priceDisplay').get(function() {
  if (!this.price || !this.price.amount) return null;
  return `$${this.price.amount}/${this.price.period}`;
});

// Pre-save middleware برای تولید slug اگر ارائه نشده باشد
serviceSchema.pre('save', function(next) {
  if (!this.slug && this.title) {
    this.slug = this.title
      .toLowerCase()
      .replace(/[^a-z0-9]+/g, '-')
      .replace(/-+/g, '-')
      .trim('-');
  }
  next();
});

// متد static برای یافتن services فعال
serviceSchema.statics.findActive = function() {
  return this.find({ isActive: true }).sort({ displayOrder: 1, createdAt: -1 });
};

module.exports = mongoose.model('Service', serviceSchema);
```

#### **مدل Blog - سیستم مدیریت محتوا**
```javascript
// models/Blog.js
const mongoose = require('mongoose');

const blogSchema = new mongoose.Schema({
  title: {
    type: String,
    required: [true, 'عنوان blog الزامی است'],
    trim: true,
    maxlength: [200, 'عنوان نمی‌تواند بیش از 200 کاراکتر باشد']
  },

  excerpt: {
    type: String,
    required: [true, 'excerpt الزامی است'],
    maxlength: [300, 'excerpt نمی‌تواند بیش از 300 کاراکتر باشد']
  },

  content: {
    type: String,
    required: [true, 'محتوای blog الزامی است']
  },

  slug: {
    type: String,
    required: true,
    unique: true,
    lowercase: true
  },

  author: {
    type: String,
    required: [true, 'نویسنده الزامی است'],
    trim: true
  },

  image: {
    type: String,
    required: [true, 'تصویر ویژه الزامی است']
  },

  category: {
    type: String,
    enum: ['fitness', 'nutrition', 'training', 'health', 'motivation', 'tips'],
    default: 'fitness'
  },

  tags: [{
    type: String,
    trim: true,
    lowercase: true
  }],

  readTime: {
    type: Number, // به دقیقه
    default: function() {
      // تخمین زمان خواندن بر اساس طول محتوا
      return Math.ceil(this.content.split(' ').length / 200);
    }
  },

  viewCount: {
    type: Number,
    default: 0
  },

  isPublished: {
    type: Boolean,
    default: false
  },

  featured: {
    type: Boolean,
    default: false
  },

  seo: {
    metaTitle: { type: String, maxlength: 60 },
    metaDescription: { type: String, maxlength: 160 },
    keywords: [{ type: String }]
  }

}, {
  timestamps: true
});

// ایندکس‌ها
blogSchema.index({ slug: 1 });
blogSchema.index({ category: 1 });
blogSchema.index({ isPublished: 1 });
blogSchema.index({ createdAt: -1 });
blogSchema.index({ tags: 1 });

// Virtual برای URL-friendly SEO
blogSchema.virtual('seoUrl').get(function() {
  return `/blog/${this.slug}`;
});

// متد برای افزایش view count
blogSchema.methods.incrementViews = function() {
  this.viewCount += 1;
  return this.save();
};

// متد static برای یافتن blogهای منتشر شده
blogSchema.statics.findPublished = function() {
  return this.find({ isPublished: true }).sort({ createdAt: -1 });
};

// Pre-save validation
blogSchema.pre('save', function(next) {
  if (!this.slug && this.title) {
    this.slug = this.title
      .toLowerCase()
      .replace(/[^a-z0-9]+/g, '-')
      .replace(/-+/g, '-')
      .trim('-');
  }

  // تولید پیش‌فرض‌های SEO اگر ارائه نشده باشند
  if (!this.seo.metaTitle && this.title) {
    this.seo.metaTitle = this.title.substring(0, 60);
  }

  if (!this.seo.metaDescription && this.excerpt) {
    this.seo.metaDescription = this.excerpt.substring(0, 160);
  }

  if (!this.seo.keywords && this.tags.length > 0) {
    this.seo.keywords = this.tags.slice(0, 5);
  }

  next();
});

module.exports = mongoose.model('Blog', blogSchema);
```

#### **مدل Team - مدیریت کاربر**
```javascript
// models/Team.js
const mongoose = require('mongoose');

const teamSchema = new mongoose.Schema({
  name: {
    type: String,
    required: [true, 'نام عضو تیم الزامی است'],
    trim: true,
    maxlength: [100, 'نام خیلی طولانی است']
  },

  email: {
    type: String,
    required: [true, 'ایمیل الزامی است'],
    unique: true,
    lowercase: true,
    match: [/^\w+([.-]?\w+)*@\w+([.-]?\w+)*(\.\w{2,3})+$/, 'لطفا ایمیل معتبر وارد کنید']
  },

  position: {
    type: String,
    required: [true, 'موقعیت الزامی است'],
    enum: ['ceo', 'trainer', 'nutritionist', 'coach', 'manager']
  },

  bio: {
    type: String,
    required: [true, 'بیوگرافی الزامی است'],
    maxlength: [500, 'بیو نمی‌تواند بیش از 500 کاراکتر باشد']
  },

  image: {
    type: String,
    required: [true, 'تصویر پروفایل الزامی است']
  },

  slug: {
    type: String,
    required: true,
    unique: true,
    lowercase: true
  },

  specializations: [{
    type: String,
    enum: ['strength', 'cardio', 'yoga', 'pilates', 'nutrition', 'boxing', 'crossfit']
  }],

  certifications: [{
    name: String,
    issuer: String,
    dateObtained: Date,
    validUntil: Date,
    credentialUrl: String
  }],

  experience: {
    years: { type: Number, min: 0 },
    summary: String
  },

  social: {
    twitter: { type: String, trim: true },
    linkedin: { type: String, trim: true },
    facebook: { type: String, trim: true },
    instagram: { type: String, trim: true },
    youtube: { type: String, trim: true }
  },

  contact: {
    phone: String,
    office: String
  },

  schedule: [{
    day: {
      type: String,
      enum: ['monday', 'tuesday', 'wednesday', 'thursday', 'friday', 'saturday', 'sunday']
    },
    startTime: String,
    endTime: String,
    available: { type: Boolean, default: true }
  }],

  isActive: {
    type: Boolean,
    default: true
  }

}, {
  timestamps: true
});

// ایندکس‌ها
teamSchema.index({ slug: 1 });
teamSchema.index({ 'specializations': 1 });
teamSchema.index({ 'position': 1 });

// Virtual برای نمایش نام کامل
teamSchema.virtual('displayName').get(function() {
  return this.name;
});

// Virtual برای available بودن امروز
teamSchema.virtual('isAvailableToday').get(function() {
  const today = new Date().toLocaleLowerCase().substring(0, 3);
  const todaySchedule = this.schedule.find(s => s.day === today);
  return todaySchedule && todaySchedule.available;
});

// متد برای گرفتن time slotهای available
teamSchema.methods.getAvailableTimes = function(day) {
  return this.schedule.filter(slot => slot.day === day && slot.available);
};

// متد static برای یافتن trainerها بر اساس specialization
teamSchema.statics.findBySpecialization = function(specialization) {
  return this.find({
    specializations: specialization,
    isActive: true
  });
};

module.exports = mongoose.model('Team', teamSchema);
```

---

## 🔧 **Technology Stack**

### **معماری Backend Stack - Robust Server Architecture**

| **لایه** | **تکنولوژی** | **هدف** | **ویژگی‌های کلیدی** |
|-----------|----------------|-------------|-----------------|
| **Runtime** | Node.js 18+ | اجرای سرور | ES6+، الگوهای Async/Await، Modules |
| **Framework** | Express.js 4.18 | فریمورک وب | Routing، Middleware، مدیریت خطا |
| **Database** | MongoDB 7.0 | ذخیره‌سازی داده | Documents شبه JSON، Schemaهای انعطاف‌پذیر |
| **ODM** | Mongoose 8.0 | مدل‌سازی داده | اعتبار سنجی Schema، Queryها، Middleware |
| **امنیت** | Helmet، CORS | محافظت | جلوگیری از XSS، محافظت CSRF |
| **اعتبار سنجی** | Express-Validator | اعتبار سنجی ورودی | Sanitization، قوانین سفارشی |
| **Authentication** | JWT، Bcrypt | احراز هویت کاربر | Authentication مبتنی بر توکن، Hashing رمز عبور |

### **معماری Frontend Stack - Modern React Architecture**

| **دسته‌بندی** | **تکنولوژی** | **هدف** | **ویژگی‌های کلیدی** |
|--------------|----------------|-------------|-----------------|
| **Framework** | React 18.2 | کتابخانه UI | کامپوننت‌ها، Hooks، Virtual DOM |
| **Routing** | React Router 6.11 | ناوبری | Client-side routing، پارامترها |
| **HTTP Client** | Axios 1.4 | فراخوانی API | Interceptorهای Request/Response |
| **ابزار Build** | Create React App | توسعه | Hot reload، بهینه‌سازی‌ها |
| **CSS Framework** | Bootstrap 5.3 | استایل‌بندی | Grid responsive، کامپوننت‌ها |
| **State** | React Hooks | مدیریت داده | useState، useEffect، Custom hooks |

### **ابزارهای توسعه و DevOps**

| **ابزار** | **هدف** | **استفاده** |
|----------|-------------|-----------|
| **Nodemon** | راه‌اندازی مجدد خودکار | توسعه Backend |
| **ESLint** | کیفیت کد | Linting JavaScript |
| **Prettier** | فرمت‌بندی کد | استایل‌سازی ثابت |
| **Git** | کنترل نسخه | مدیریت کد منبع |
| **PM2** | Process manager | Deployment تولید |
| **MongoDB Compass** | GUI پایگاه داده | Visualisation داده |

---

## 📡 **مستندسازی REST API**

### **آدرس پایه API**
```
تولید: https://api.fitmas.com/api/v1
توسعه: http://localhost:5000/api
```

### **Headers احراز هویت**
```
Authorization: Bearer <jwt_token>
Content-Type: application/json
```

### **کدهای وضعیت HTTP**
- **200 OK** - موفق
- **201 Created** - Resource ایجاد شد
- **400 Bad Request** - ورودی نامعتبر
- **401 Unauthorized** - احراز هویت الزامی
- **403 Forbidden** - دسترسی ممنوع
- **404 Not Found** - Resource یافت نشد
- **429 Too Many Requests** - محدود شده نرخ
- **500 Internal Server Error** - خطای سرور

### **API Services Endpoints**

#### **GET /api/services - لیست Services**
```bash
GET /api/services?page=1&limit=10&category=fitness&active=true
```

**پارامترهای Query:**
- `page` (number): شماره صفحه برای صفحه‌بندی (پیش‌فرض: 1)
- `limit` (number): آیتم‌ها در هر صفحه (پیش‌فرض: 10، حداکثر: 50)
- `category` (string): فیلتر بر اساس دسته‌بندی
- `active` (boolean): فقط Services فعال را فیلتر کن

**پاسخ:**
```json
{
  "success": true,
  "data": [
    {
      "_id": "64f1a2b3c4d5e6f7g8h9i0j",
      "title": "آموزش Personal Training",
      "description": "جلسات آموزشی شخصی...",
      "slug": "personal-training",
      "icon": "dumbbell",
      "category": "fitness",
      "features": ["برنامه‌های سفارشی", "ردیابی پیشرفت"],
      "price": {
        "amount": 75,
        "currency": "USD",
        "period": "session"
      },
      "isActive": true,
      "createdAt": "2024-01-15T10:00:00.000Z"
    }
  ],
  "pagination": {
    "page": 1,
    "limit": 10,
    "total": 25,
    "pages": 3
  }
}
```

#### **POST /api/services - ایجاد Service**
```bash
POST /api/services
Authorization: Bearer <admin_token>
```

**بدنه درخواست:**
```json
{
  "title": "آموزش HIIT",
  "description": "High-Intensity Interval Training برای نتایج حداکثری",
  "icon": "running",
  "category": "fitness",
  "features": ["جلسات 45 دقیقه‌ای", "مانیتورینگ ضربان قلب"],
  "price": {
    "amount": 50,
    "period": "session"
  }
}
```

#### **GET /api/services/:slug - دریافت Service بر اساس Slug**
```bash
GET /api/services/personal-training
```

#### **PUT /api/services/admin/:id - بروزرسانی Service**
```bash
PUT /api/services/admin/64f1a2b3c4d5e6f7g8h9i0j
Authorization: Bearer <admin_token>
```

#### **DELETE /api/services/admin/:id - حذف Service**
```bash
DELETE /api/services/admin/64f1a2b3c4d5e6f7g8h9i0j
Authorization: Bearer <admin_token>
```

### **API Blog Endpoints**

#### **GET /api/blogs - لیست پست‌های Blog**
**پارامترها:** صفحه‌بندی، فیلترینگ دسته‌بندی، جستجو

#### **GET /api/blogs/:slug - دریافت پست Blog**

#### **POST /api/blogs - ایجاد پست Blog** (Admin)

#### **PUT /api/blogs/admin/:id - بروزرسانی پست Blog** (Admin)

#### **DELETE /api/blogs/admin/:id - حذف پست Blog** (Admin)

---

## 🚀 **نصب و راه‌اندازی سریع**

### **پیش‌نیازها**
- **Node.js** (v18.0.0 یا بالاتر) - https://nodejs.org/
- **MongoDB** (نصب محلی یا حساب MongoDB Atlas)
- **Git** - https://git-scm.com/
- **Visual Studio Code** (IDE پیشنهادی)
- **مرورگر وب مدرن** (Chrome, Firefox, Safari, Edge)

### **راهنمای شروع سریع**

1. **Clone و Navigate:**
```bash
git clone https://github.com/ahamzehamin/fitmas-mern.git
cd fitmas-mern
```

2. **نصب Backend:**
```bash
cd backend
npm install
cp .env.example .env
# فایل .env را ویرایش کنید با connection string MongoDB خود
npm run dev
# Backend روی http://localhost:5000 اجرا می‌شود
```

3. **نصب Frontend:**
```bash
cd frontend
npm install
npm start
# Frontend روی http://localhost:3000 اجرا می‌شود
```

---

